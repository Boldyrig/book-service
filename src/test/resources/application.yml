spring:
  datasource:
    url: jdbc:h2:mem:book-service-test
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
      schema-locations: schema-test.sql
      data-locations: data-test.sql
  h2:
    console:
      enabled: true
  liquibase:
    enabled: false